<?php

/*

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Example extends Model
{
    public $incrementing = true;
    public $timestamps = false;
    protected $table = 'example';
    protected $primaryKey = 'id';
    protected $guarded = [];
    protected $hidden = [];

    // Relationships

    // When "my_id" is my primary key
    public function model()
    {
        return $this->hasOne('App\Models\Model', 'their_id', 'my_id');
    }

    // When "my_id" is not a primary key
    public function model()
    {
        return $this->belongsTo('App\Models\Model', 'my_id', 'their_id');
    }

    public function model()
    {
        return $this->hasMany('App\Models\Model', 'their_id', 'my_id');
    }

    // Scopes

    // Methods

    // Static
}

*/
